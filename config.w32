// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("yajl", "for yajl support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("yajl", "enable yajl support", "no");

if (PHP_YAJL != "no") {
	EXTENSION("yajl", "yajl.c");
}

